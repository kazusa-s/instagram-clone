<% @posts.each do |post| %>
  <div class="col-md-8 col-md-2 mx-auto">
    <div class="card-wrap">
      <div class="card">
        <div class="card-header align-items-center d-flex">

          <% if post.user_id == current_user.id %>
            <%= link_to "delete", post_path(post), method: :delete, data: {confirm: '削除しますか？'}, class: "ml-auto mx-0 my-auto"  %>
          <% end %>
        </div>


        <%= link_to post_path(post), remote: true do %>
          <%= image_tag post.photos.first.image.url(:medium), class: "card-img-top", "data-toggle": "modal" , "data-target": "#exampleModalLong"%>
        <% end %>

        <!--modal window-->
        <div class="modal fade" id="exampleModalLong" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true"></div>
        <!--modal window-->

        <div class="card-body">
          <div class="row parts">


          <div id="like-icon-post-<%= post.id.to_s %>">
          </div>

            <%= link_to "", "#", class: "comment" %>
          </div>


          <div>
            <span><strong><%= post.user.name %></strong></span>
            <span><%= post.caption %></span>



          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

